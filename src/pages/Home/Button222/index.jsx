import './index.css'

const Button222 = (props) => {
    return ( 
        <>
          <button className='home-button'   onClick={props.lstorage} >{props.title}</button>
        </>

     );
}
 
export default Button222;